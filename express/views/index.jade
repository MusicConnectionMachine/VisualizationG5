extends layout

block content
  div.container
    div.content
      h1= title
      p Welcome to #{title}
      // Container for the react application
      div#react-app(data-host="http://localhost:3000/api/v1")
      // Load the script after the react container has been mounted to DOM

  script(src="/react/homeApp.bundle.js")
